const query = (selector: string): HTMLElement =>
    document.querySelector(selector) as HTMLElement

export function addLogoIcon(logo: string): void {
    const branding = query(".branding")
    if(branding) {
        const img = branding.querySelector("img") as HTMLImageElement
        if(img) {
            img.src = logo
        }
    }
}
